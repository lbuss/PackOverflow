PackOverflow.Collections.Users = Backbone.Collection.extend({
  model: PackOverflow.Models.Users,

  initialize: function(models, options) {
    // if (options.question){
//       this.question = options.question;
//     } else {
//       this.answer = options.answer;
//     }
  }

});